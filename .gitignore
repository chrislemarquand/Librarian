# Xcode
xcuserdata/
*.xcuserstate
DerivedData/

# Package resolution — managed by Xcode, not tracked in git.
# Local packages (SharedUI) are live references; re-resolution is only
# needed when a package manifest changes, which Xcode handles automatically.
Librarian.xcodeproj/project.xcworkspace/xcshareddata/swiftpm/Package.resolved
